// Retained as a no-op so existing imports do not break while the redundant
// homepage "Pilot Schools" module is intentionally removed from display.
export default function PilotSchools() {
  return null;
}
